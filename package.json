{
  "name": "health-tracker-vercel-deploy",
  "version": "1.0.0",
  "scripts": {
    "build": "cd mobile-app && npm install && npx expo export -p web && cd .. && rm -rf public && mv mobile-app/dist public"
  }
}
